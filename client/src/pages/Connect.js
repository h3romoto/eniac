import styled from "styled-components";
import Terminal from "../components/CmdTerminal";

const Connect = () => {

  return (
    <div>
        <Terminal />
    </div>
  );
};

export default Connect;